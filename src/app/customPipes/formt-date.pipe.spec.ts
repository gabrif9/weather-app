import { FormtDatePipe } from './formt-date.pipe';

describe('FormtDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormtDatePipe();
    expect(pipe).toBeTruthy();
  });
});
